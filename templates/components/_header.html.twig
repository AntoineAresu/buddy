{% set user = app.user %}
{% set route_name = app.request.attributes.get('_route')%}
<nav class="navbar navbar-expand-lg fixed-top bg-dark p-4">
    <span class="navbar-brand text-white m-0">
        <twig:ux:icon name="mdi:paw" class="fs-1"/>
        {% if not user %}
            <span class="ms-3">
                {{ 'app_name'|trans }}
            </span>
        {% endif %}
    </span>
    {% if user %}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                {% if 'list_dogs' != route_name %}
                    <li class="nav-item">
                        <a
                            class="nav-link text-white mx-4"
                            href="{{ path('list_dogs') }}"
                        >
                            <twig:ux:icon name="mdi:chevron-left" class="fs-3"/>
                            {{ 'my_dogs'|trans }}
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <a class="nav-link text-white mx-4" href="{{ path('app_logout') }}">Logout</a>
    {% endif %}
</nav>
